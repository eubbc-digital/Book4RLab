import { CardElevationDirective } from './card-elevation.directive';

describe('CardElevationDirective', () => {
  it('should create an instance', () => {
    const directive = new CardElevationDirective();
    expect(directive).toBeTruthy();
  });
});
